Symfony ObjectMapper in several situations.

Installation:

• Install ddev. (https://ddev.com/)

• Clone the repository.

• `ddev start` (inside project directory)

• `ddev composer install`

Run these tests in your terminal by:

• `ddev console app:test-1`

• `ddev console app:test-2`

• `ddev console app:test-...`
